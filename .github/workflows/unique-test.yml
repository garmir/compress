name: Unique Test - compress
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - compress
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: compress ===" > unique-test-compress.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-compress.txt
            echo "Test ID: TEST_196" >> unique-test-compress.txt
            echo "Timestamp: $(date)" >> unique-test-compress.txt
            curl -s ifconfig.me >> unique-test-compress.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-compress.txt
          '

      - name: Ubuntu Fallback - compress
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for compress" > fallback-compress.txt
          curl -s ifconfig.me >> fallback-compress.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-compress
          path: "*.txt"
          retention-days: 2